import { useEffect, useState } from 'react'; import { io } from 'socket.io-client'; import { AnimatePresence, motion } from 'framer-motion'; import { Activity, AlertTriangle, CheckCircle2, ChevronDown, CloudRain, Crosshair, Gauge, Layers3, Menu, Package, Radio, ShieldCheck, Truck, Wifi, X } from 'lucide-react'; import { api, socketUrl } from './services/api'; import MapView from './components/MapView'; import IncidentDrawer from './components/IncidentDrawer'; import Telemetry from './components/Telemetry'; import RouteCard from './components/RouteCard';
const demoPlaces=[{type:'hospital',name:'GMCH Emergency',position:[91.7362,26.1445]},{type:'hospital',name:'Silchar Medical College',position:[92.7789,24.8333]},{type:'warehouse',name:'NER Relief Depot',position:[91.78,26.18]},{type:'helipad',name:'Shillong Helipad',position:[91.9,25.59]}]; const blankRoute={primary:{geojson:{coordinates:[]},eta:0,distance:0,risk:0},bypass:{geojson:{coordinates:[]},eta:0,distance:0,risk:0}};
export default function App(){const [routes,setRoutes]=useState(blankRoute),[places,setPlaces]=useState(demoPlaces),[drawer,setDrawer]=useState(false),[offline,setOffline]=useState(!navigator.onLine),[incident,setIncident]=useState(null),[metrics,setMetrics]=useState({rainfall:86,soil:54,slope:18,risk:.24}),[origin,setOrigin]=useState('Guwahati'),[destination,setDestination]=useState('Silchar'),[priority,setPriority]=useState('P1 Medical'),[loading,setLoading]=useState(false),[toast,setToast]=useState(''),[cargo,setCargo]=useState({id:'CVY-2048',type:'Vaccines',priority:'P1',temperature:'+4.1 C',eta:165,location:'Guwahati'}),[active,setActive]=useState('primary');
useEffect(()=>{api.get('/demo/state').then(({data})=>{setRoutes(data.routes);setPlaces(data.places);setCargo(data.cargo)}).catch(()=>{}); const socket=io(socketUrl,{autoConnect:true}); socket.on('route:updated',setRoutes); socket.on('risk:update',r=>setMetrics(m=>({...m,risk:r.riskScore}))); socket.on('cargo:update',d=>setCargo(c=>({...c,...d}))); const online=()=>{setOffline(false); syncQueue()}; const offlineFn=()=>setOffline(true); window.addEventListener('online',online);window.addEventListener('offline',offlineFn); return()=>{socket.disconnect();window.removeEventListener('online',online);window.removeEventListener('offline',offlineFn)}},[]);
async function syncQueue(){const queue=JSON.parse(localStorage.getItem('ner-queue')||'[]'); for(const item of queue){try{await api.post('/incidents',item); localStorage.setItem('ner-queue',JSON.stringify(queue.filter(x=>x!==item))); setToast('INCIDENT SYNCED SUCCESSFULLY')}catch{break}}}
async function calculate(){setLoading(true);try{const {data}=await api.post('/routes/calculate',{origin,destination,priority});setRoutes(data);setActive('primary')}catch{}finally{setLoading(false)}}
async function submitIncident(e){e.preventDefault();const f=new FormData(e.currentTarget);const [lng,lat]=f.get('coordinates').split(',').map(Number);const payload={type:f.get('type'),severity:f.get('severity'),coordinates:[lng,lat],online:!offline,riskInput:{rainfall_24h:metrics.rainfall+100,soil_moisture:Math.min(98,metrics.soil+30),slope_angle:32,elevation:400,road_condition:1},origin,destination}; if(offline){const q=JSON.parse(localStorage.getItem('ner-queue')||'[]');localStorage.setItem('ner-queue',JSON.stringify([...q,payload]));setToast('OFFLINE — INCIDENT QUEUED');setIncident(payload);setDrawer(false);return} setLoading(true);try{const {data}=await api.post('/incidents',payload);setIncident(data.incident);setRoutes(data.routes);setMetrics({rainfall:metrics.rainfall+100,soil:Math.min(98,metrics.soil+30),slope:32,risk:data.risk.riskScore});setCargo(c=>({...c,eta:data.routes.bypass.eta,location:'AI BYPASS ROUTE'}));setActive('bypass');setToast('INCIDENT VERIFIED · CARGO REROUTED');setDrawer(false)}catch{setToast('Unable to broadcast incident')}finally{setLoading(false)}}
async function simulate(){setMetrics({rainfall:242,soil:91,slope:34,risk:.92});setToast('SIMULATION: AI RISK ESCALATING');await new Promise(r=>setTimeout(r,900));await submitIncident({preventDefault(){},currentTarget:{}}).catch(()=>{}); const fake={type:'Landslide',severity:'CRITICAL',coordinates:[92.7789,24.8333],riskInput:{rainfall_24h:242,soil_moisture:91,slope_angle:34,elevation:400,road_condition:1},origin,destination}; try{const {data}=await api.post('/incidents',fake);setIncident(data.incident);setRoutes(data.routes);setCargo(c=>({...c,eta:data.routes.bypass.eta,location:'AI BYPASS ROUTE'}));setActive('bypass');setToast('LANDSLIDE CONFIRMED · SAFE BYPASS ACTIVE')}catch{setToast('Demo API offline: queued locally')}}
return <div className="app-shell"><header className="topbar"><div className="brand"><div className="brand-mark"><ShieldCheck size={23}/></div><div><div className="brand-name">NER<span>-</span>SHIELD</div><div className="brand-sub">MDoNER LOGISTICS GRID</div></div></div><div className="system-pills"><span><i className="live-dot"/> WEATHER <b>LIVE</b></span><span><Activity size={14}/> TELEMETRY <b>ACTIVE</b></span><span><Truck size={14}/> ACTIVE CONVOYS <b>05</b></span><span className="critical"><AlertTriangle size={14}/> CRITICAL INCIDENTS <b>{incident?'01':'00'}</b></span></div><div className="top-actions"><span className="operator"><span className="avatar">AR</span><span><b>ANANYA RAO</b><small>COMMAND OFFICER</small></span></span><button className="icon-btn mobile-menu"><Menu size={18}/></button></div></header><main className="workspace"><aside className="sidebar"><div className="section-label"><span>ROUTE DISPATCH</span><span className="demo-badge">DEMO DATA</span></div><div className="field"><label>ORIGIN</label><div className="select-wrap"><Crosshair size={15}/><select value={origin} onChange={e=>setOrigin(e.target.value)}><option>Guwahati</option><option>Shillong</option><option>Silchar</option><option>Imphal</option><option>Aizawl</option><option>Kohima</option><option>Agartala</option><option>Siliguri</option></select><ChevronDown size={14}/></div></div><div className="field"><label>DESTINATION</label><div className="select-wrap"><Layers3 size={15}/><select value={destination} onChange={e=>setDestination(e.target.value)}><option>Silchar</option><option>Guwahati</option><option>Shillong</option><option>Imphal</option><option>Aizawl</option><option>Kohima</option><option>Agartala</option><option>Siliguri</option></select><ChevronDown size={14}/></div></div><div className="priority-head"><label>CARGO PRIORITY</label><span>SELECT ONE</span></div><div className="priority-tabs">{['P1 Medical','P2 Food','P3 Construction'].map(p=><button key={p} className={priority===p?'active':''} onClick={()=>setPriority(p)}>{p.split(' ')[0]}<small>{p.slice(3)}</small></button>)}</div><button className="route-btn" onClick={calculate} disabled={loading}><Gauge size={17}/>{loading?'CALCULATING...':'CALCULATE SAFE ROUTE'}<span>↗</span></button><div className="routes-title"><span>AVAILABLE ROUTES</span><span>LIVE</span></div><RouteCard title="PRIMARY ROUTE" route={routes.primary} color="#ff5a52" selected={active==='primary'} onClick={()=>setActive('primary')}/><RouteCard title="AI BYPASS ROUTE" route={routes.bypass} color="#45e0d0" selected={active==='bypass'} onClick={()=>setActive('bypass')}/><div className="cargo-card"><div className="cargo-title"><span><Package size={16}/> ACTIVE CARGO</span><span className="pulse-label">TRACKING</span></div><strong>{cargo.type}</strong><div className="cargo-meta"><span><b>{cargo.id}</b> · {cargo.priority}</span><span className="temp">{cargo.temperature}</span></div><div className="cargo-progress"><i style={{width:active==='bypass'?'36%':'18%'}}/></div><div className="cargo-foot"><span>{cargo.location}</span><span>ETA <b>{cargo.eta} min</b></span></div></div></aside><section className="map-area"><div className="map-toolbar"><div><span className="eyebrow">NORTH EASTERN REGION</span><h1>Logistics Command Map</h1></div><div className="map-tools"><button className="tool-active"><span className="map-dot cyan"/> ROUTE LAYERS</button><button><CloudRain size={15}/> WEATHER RADAR</button></div></div><div className="map-wrap"><MapView routes={routes} places={places} incident={incident} cargo={cargo}/><div className="map-legend"><span><i className="legend-line primary"/> PRIMARY</span><span><i className="legend-line bypass"/> AI BYPASS</span><span><i className="legend-square blocked"/> BLOCKED</span><span><i className="legend-dot hospital"/> FACILITY</span></div><div className="map-status"><span><Wifi size={14}/> LIVE NETWORK</span><small>LAST SYNC 14:32:08 IST</small></div></div></section></main><div className="bottom-bar"><Telemetry metrics={metrics}/><div className="incident-actions"><button className="simulate-btn" onClick={simulate}><span className="sim-icon">◉</span> SIMULATE LANDSLIDE</button><button className="report-btn" onClick={()=>setDrawer(true)}><AlertTriangle size={17}/> REPORT HAZARD</button><div className="connectivity"><span className={offline?'offline-dot':'live-dot'}/>{offline?'OFFLINE MODE':'ONLINE · SYNCED'}</div></div></div><AnimatePresence><IncidentDrawer open={drawer} onClose={()=>setDrawer(false)} onSubmit={submitIncident} offline={offline}/></AnimatePresence>{toast&&<motion.div initial={{y:30,opacity:0}} animate={{y:0,opacity:1}} className="toast" onAnimationComplete={()=>setTimeout(()=>setToast(''),3200)}><CheckCircle2 size={17}/>{toast}<X size={15}/></motion.div>}</div>}
